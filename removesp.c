//leia uma frase e crie uma sring sem os espaços
#include <stdio.h>
#define TAM 100
int main()
{

    char frase[TAM], semEsp[TAM];
    int i, j = 0;

    printf("Digite uma frase: ");
    gets(frase);
    fflush(stdin); //
}